# constraint-preserve-bake
A tool that captures constraint-driven motion (e.g. tweaking a control by constraining it to a locator) into an additive AnimLayer, without disturbing the original animation underneath.
